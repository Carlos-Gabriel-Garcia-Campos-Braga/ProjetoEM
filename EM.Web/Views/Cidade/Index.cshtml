@model IEnumerable<EM.Domain.Cidade>

@{
    ViewData["Title"] = "Gerenciar Cidades";
}

<div class="container mt-4">
    @if (TempData["ErroForeignKey"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong><i class="bi bi-exclamation-triangle"></i> Erro!</strong>
            @TempData["ErroForeignKey"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["Erro"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong><i class="bi bi-exclamation-triangle"></i> Erro!</strong>
            @TempData["Erro"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-primary">Gerenciar Cidades</h2>
        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Adicionar Cidade
        </a>
    </div>

    <div class="card">
        <div class="card-body">
            @if (Model != null && Model.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover table-striped">
                        <thead class="table-primary">
                            <tr>
                                <th class="text-center">ID</th>
                                <th class="text-center">Nome da Cidade</th>
                                <th class="text-center">UF</th>
                                <th class="text-center" style="width: 180px;">Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var cidade in Model)
                            {
                                <tr>
                                    <td class="text-center">@cidade.Id</td>
                                    <td class="text-center">@cidade.NomeDaCidade</td>
                                    <td class="text-center">@cidade.UF</td>
                                    <td class="text-center">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@cidade.Id" class="btn btn-sm btn-warning">
                                                <i class="bi bi-pencil"></i> Editar
                                            </a>
                                            <button type="button" class="btn btn-sm btn-danger" onclick="confirmarExclusao(@cidade.Id, '@cidade.NomeDaCidade')">
                                                <i class="bi bi-trash"></i> Excluir
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="alert alert-info text-center" role="alert">
                    <i class="bi bi-info-circle"></i> Nenhuma cidade cadastrada.
                </div>
            }
        </div>
    </div>
</div>

<form id="formExcluir" method="post" style="display: none;">
    @Html.AntiForgeryToken()
</form>

<div class="modal fade" id="modalConfirmarExclusao" tabindex="-1" aria-labelledby="modalConfirmarExclusaoLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalConfirmarExclusaoLabel">
                    <i class="bi bi-exclamation-triangle text-warning"></i> Confirmar Exclusão
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>Tem certeza que deseja excluir a cidade <strong id="nomeCidadeExclusao"></strong> (ID: <strong id="idCidadeExclusao"></strong>)?</p>
                <p class="text-danger mb-0"><small>Esta ação não pode ser desfeita.</small></p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle"></i> Cancelar
                </button>
                <button type="button" class="btn btn-danger" id="btnConfirmarExclusao">
                    <i class="bi bi-trash"></i> Excluir
                </button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="modalErroForeignKey" tabindex="-1" aria-labelledby="modalErroForeignKeyLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="modalErroForeignKeyLabel">
                    <i class="bi bi-exclamation-triangle"></i> Erro ao Excluir
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="mb-0">Não é possível excluir a cidade pois possui um aluno vinculado a ela.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-check-circle"></i> Entendi
                </button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        let idParaExcluir = null;

        function confirmarExclusao(id, nome) {
            idParaExcluir = id;
            document.getElementById('nomeCidadeExclusao').textContent = nome;
            document.getElementById('idCidadeExclusao').textContent = id;
            
            const modal = new bootstrap.Modal(document.getElementById('modalConfirmarExclusao'));
            modal.show();
        }

        document.getElementById('btnConfirmarExclusao').addEventListener('click', function() {
            if (idParaExcluir !== null) {
                const form = document.getElementById('formExcluir');
                form.action = '@Url.Action("Delete", "Cidade")/' + idParaExcluir;
                form.submit();
            }
        });

        document.addEventListener('DOMContentLoaded', function() {
            const erroForeignKey = '@TempData["ErroForeignKey"]';
            if (erroForeignKey && erroForeignKey !== '') {
                const modalErro = new bootstrap.Modal(document.getElementById('modalErroForeignKey'));
                modalErro.show();
            }
        });
    </script>
}
